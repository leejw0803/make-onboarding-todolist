import styled from "styled-components";

const LogoStyle = {
  Logo: styled.img.attrs({ alt: "make logo", src: "images/img-logo.png" })`
    width: 115px;
    height: 48px;
  `,
};

export default LogoStyle;
